# ClipVault 设计文档

## 目标

1. **简单好用不卡**：Alfred 式体验——热键呼出、即输即搜、回车复制，支持图片。
2. **安全**：把「剪贴板历史 = 明文密码库」这个通病堵住。
3. **可设置**：保留时长、图片开关与大小上限、文本大小上限等。

## 威胁模型

**防的是：**
- 历史文件以明文躺在磁盘上，被 Time Machine / 云同步 / 换机迁移 / 同机其他用户带走
- 密码管理器复制的密码进入历史
- 手滑复制的 token / key 长期留存、被肩窥（列表明文展示）
- 剪贴板工具本身回传数据（本项目无网络代码，可审计）

**不防的是（明确不承诺）：**
- 已拿到当前登录用户完整权限的攻击者——密钥与数据同机同用户，模式上无解；
  对这种场景的缓解是「仅内存」模式（退出即清空、磁盘零痕迹）
- 其他 App 直接读系统剪贴板（那是 macOS 剪贴板本身的行为，macOS 15.4+ 系统层已开始管控）

## 关键决策与取舍

### 存储加密：文件密钥而非 Keychain
AES-256-GCM，密钥 32 字节随机、`0600` 存放在数据目录。**为什么不用 Keychain**：
ad-hoc 签名的 App 每次重装/重建签名都变，Keychain 会反复弹「允许访问」——违背「不弹窗」目标。
文件密钥防的是备份/同步/翻文件级泄露；要求更高就切「仅内存」。此取舍已写进代码注释与 README。

### 全局热键：Carbon RegisterEventHotKey
唯一不需要任何权限的全局热键 API（CGEventTap 需要辅助功能授权）。默认 ⌃⇧V——
刻意避开 ⇧⌘V（很多 App 的「粘贴并匹配样式」），提供 4 个预设可切换。

### 剪贴板监听：changeCount 轮询
macOS 没有剪贴板变更通知，Maccy / Alfred 都是轮询。每 tick 只比较一个整数，350ms 间隔
（e2e 测试下调到 80ms）。图片转码 / 缩略图在后台队列做，主线程零重活——保证「不卡」。

### 敏感内容三道闸
1. **Concealed/Transient 标记**（nspasteboard.org 约定）：密码管理器主动声明，最可靠，默认跳过；
2. **排除应用**：按前台 App bundle id 拒绝，默认内置主流密码管理器（双保险，因为部分旧版不打标记）；
3. **凭证格式检测**：gitleaks 风格正则子集（AWS/GitHub/GitLab/Slack/Stripe/OpenAI/JWT/PEM/Bearer/
   `api_key=` 赋值式）。策略三选一：不记录 / 遮罩（默认）/ 关闭。遮罩条目列表只显示圆点、
   不参与正文搜索（防止用搜索探测内容）、dump 工具也只输出遮罩。

### 数据格式
```
~/Library/Application Support/ClipVault/   (0700)
  key.bin       32B 随机密钥 (0600)
  history.enc   条目元数据 JSON → AES-GCM (0600)。文本内联；图片只有尺寸+缩略图
  blobs/<uuid>  图片完整 PNG → AES-GCM 逐文件
```
元数据整体重写 + 0.5s debounce；blob 按需读取（点击复制时才解密大图）；
保存时清理孤儿 blob。退出时强制落盘一次。

### 去重与清理
同内容（SHA-256）再次复制 → 旧条目移除、新条目置顶。过期（保留时长）与超额（最大条数）
在每次插入和每分钟定时器里清理，同时删除对应 blob。

### 「仅内存」模式
关闭加密落盘时：磁盘数据即刻擦除，图片捞回内存缓存；重新开启时全量回写。

## 已知限制 / 后续可做

- 富文本（RTF/HTML）只保留纯文本表示；文件复制只记路径不复制内容
- 无置顶（pin）、无多选、无 iCloud 同步（有意不做同步——安全产品不出机器）
- 热键仅 4 个预设，未做自由录制
- ad-hoc 签名：同事安装需右键打开一次；如后续要正经分发可加 Developer ID + notarization
- macOS 15.4+ 的剪贴板隐私提醒需要用户在系统设置里允许一次

## 测试策略

- **单元（32 个）**：CapturePolicy 全分支、SecretDetector 正/负样本 + 大文本性能、
  HistoryStore 去重/过期/限额/搜索/敏感不可搜、EncryptedDiskStore 往返/无明文/权限/wipe/坏密钥
- **端到端（9 项断言）**：真实启动 App、真实写系统剪贴板（普通文本、2MB 大文本、假 token、
  Concealed 标记、小图、大图），kill 后用 cvdump 解密校验捕获结果，并 grep 数据目录确认无明文
